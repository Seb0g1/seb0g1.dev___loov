# РўРµС…РЅРѕ РҐР°Р»СЏРІР° Admin

Р•РґРёРЅР°СЏ Р°РґРјРёРЅ-РїР°РЅРµР»СЊ Рё backend РґР»СЏ 4 Р°РІС‚РѕРЅРѕРјРЅС‹С… Telegram-РїСЂРѕРµРєС‚РѕРІ:

1. РЈСЋС‚ Р·Р° РєРѕРїРµР№РєРё
2. Р–РµР»РµР·РЅС‹Р№ Р’РёС‚Р°РјРёРЅ
3. РўРѕС‡РєР° СЃС‚РёР»СЏ
4. РўРµС…РЅРѕ С…Р°Р»СЏРІР°

## Р§С‚Рѕ СѓРјРµРµС‚ РїСЂРѕРµРєС‚

- РёРјРїРѕСЂС‚ Рё С„РёР»СЊС‚СЂР°С†РёСЏ С‚РѕРІР°СЂРѕРІ
- СЃРєРѕСЂРёРЅРі РІС‹РіРѕРґРЅС‹С… РїРѕР·РёС†РёР№
- Р°РЅС‚РёРґСѓР±Р»СЊ
- РіРµРЅРµСЂР°С†РёСЏ С‚РµРєСЃС‚Р° Рё РѕР±Р»РѕР¶РµРє
- С‡РµСЂРЅРѕРІРёРєРё Рё СЂСѓС‡РЅРѕРµ СЃРѕРіР»Р°СЃРѕРІР°РЅРёРµ
- Р°РІС‚РѕРїСѓР±Р»РёРєР°С†РёСЏ РІ Telegram
- СЂРµРєР»Р°РјРЅС‹Рµ Р·Р°СЏРІРєРё, СЃС‡РµС‚Р° Рё РѕРїР»Р°С‚Р°
- РїСЂРѕРІРµСЂРєР° РѕРїСѓР±Р»РёРєРѕРІР°РЅРЅС‹С… РїРѕСЃС‚РѕРІ
- Р°РЅР°Р»РёС‚РёРєР°, Р»РѕРіРё Рё РЅР°СЃС‚СЂРѕР№РєРё

## РџСЂРѕРґР°РєС€РЅ-СЃС…РµРјР°

- РїР°РЅРµР»СЊ РѕС‚РєСЂС‹РІР°РµС‚СЃСЏ РЅР° `http://ai.sebog1.ru:7070`
- С„СЂРѕРЅС‚РµРЅРґ РѕС‚РґР°С‘С‚СЃСЏ С‡РµСЂРµР· nginx
- API РїСЂРѕРєСЃРёСЂСѓРµС‚СЃСЏ РЅР° РІРЅСѓС‚СЂРµРЅРЅРёР№ backend
- Р±Р°Р·Р° РґР°РЅРЅС‹С… СЂР°Р±РѕС‚Р°РµС‚ РІ PostgreSQL РІРЅСѓС‚СЂРё Docker Compose

## Р›РѕРєР°Р»СЊРЅС‹Р№ Р·Р°РїСѓСЃРє

1. РЎРєРѕРїРёСЂСѓР№С‚Рµ `.env.example` РІ `.env`
2. Р—Р°РїРѕР»РЅРёС‚Рµ РєР»СЋС‡Рё Telegram, РїР»Р°С‚РµР¶РµР№ Рё РЅРµР№СЂРѕСЃРµС‚РµР№
3. Backend:

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

4. Frontend:

```bash
cd frontend
npm install
npm run dev
```

РџР°РЅРµР»СЊ:

- backend API: `http://localhost:8000`
- frontend: `http://localhost:5173`

## Docker / РїСЂРѕРґ

```bash
COMPOSE_PROJECT_NAME=seb0g1loov docker compose up -d --build
```

РџРѕСЃР»Рµ СЃС‚Р°СЂС‚Р° РїР°РЅРµР»СЊ РґРѕСЃС‚СѓРїРЅР° РЅР°:

- `http://ai.sebog1.ru:7070`

## Р§С‚Рѕ РІР°Р¶РЅРѕ РІ `.env`

- `TEXT_ENGINE=openrouter`
- `OPENROUTER_*` РґР»СЏ С‚РµРєСЃС‚Р°
- `IMAGE_ENGINE=codex_sale`
- `CODEX_SALE_*` РґР»СЏ РёР·РѕР±СЂР°Р¶РµРЅРёР№
- `TELEGRAM_ADMIN_ID` РґР»СЏ СЃРѕРіР»Р°СЃРѕРІР°РЅРёСЏ РїРѕСЃС‚РѕРІ
- `TELEGRAM_BOT_TOKEN` РґР»СЏ Р±РѕС‚Р°
- `YOOKASSA_*` Рё `CRYPTOBOT_*` РґР»СЏ РѕРїР»Р°С‚С‹ СЂРµРєР»Р°РјС‹

## Telegram

- СЃРѕР·РґР°Р№С‚Рµ Р±РѕС‚Р° С‡РµСЂРµР· `@BotFather`
- СѓРєР°Р¶РёС‚Рµ `TELEGRAM_BOT_TOKEN`
- РґРѕР±Р°РІСЊС‚Рµ Р±РѕС‚Р° Р°РґРјРёРЅРѕРј РІ РєР°РЅР°Р»С‹
- СѓРєР°Р¶РёС‚Рµ `TELEGRAM_ADMIN_ID`
- РїСЂРѕРІРµСЂСЊС‚Рµ С‚РѕРєРµРЅ РєРЅРѕРїРєРѕР№ РІ РїР°РЅРµР»Рё

## РќРµР№СЂРѕСЃРµС‚Рё

РџРѕРґРґРµСЂР¶РёРІР°СЋС‚СЃСЏ СЂРµР¶РёРјС‹:

- `openrouter` РґР»СЏ С‚РµРєСЃС‚Р°
- `codex_sale` РґР»СЏ РёР·РѕР±СЂР°Р¶РµРЅРёР№
- fallback-СЂРµР¶РёРјС‹ РІРЅСѓС‚СЂРё backend

## Р РµРєР»Р°РјРЅС‹Рµ Р·Р°СЏРІРєРё

- РїРѕР»СЊР·РѕРІР°С‚РµР»СЊ РѕС‚РїСЂР°РІР»СЏРµС‚ Р·Р°СЏРІРєСѓ РІ Р±РѕС‚
- Р°РґРјРёРЅ РІРёРґРёС‚ РµС‘ РІ РІРµР±Рµ Рё РІ Telegram
- РґРѕСЃС‚СѓРїРЅС‹ РєРЅРѕРїРєРё `РћРґРѕР±СЂРёС‚СЊ`, `РћС‚РјРµРЅРёС‚СЊ`, `РџРµСЂРµРґРµР»Р°С‚СЊ`
- РїРѕСЃР»Рµ РѕРїР»Р°С‚С‹ СЃРѕР·РґР°С‘С‚СЃСЏ СЃС‡С‘С‚ Рё СЃС‚Р°С‚СѓСЃ СѓС…РѕРґРёС‚ РІ `РІ СЂР°Р±РѕС‚Рµ`
- РїРѕСЃР»Рµ РїСѓР±Р»РёРєР°С†РёРё РѕС‚РїСЂР°РІР»СЏРµС‚СЃСЏ СЃСЃС‹Р»РєР° РЅР° РїРѕСЃС‚

## РљР°Рє Р·Р°Р»РёС‚СЊ РЅР° СЃРµСЂРІРµСЂ

РЎРЅР°С‡Р°Р»Р° Р·Р°РїСѓС€СЊС‚Рµ РєРѕРґ РёР· Р»РѕРєР°Р»СЊРЅРѕР№ РјР°С€РёРЅС‹ РІ GitHub. Р•СЃР»Рё СЂРµРїРѕР·РёС‚РѕСЂРёР№ Р±С‹Р» РїСѓСЃС‚РѕР№, `git clone` РЅР° СЃРµСЂРІРµСЂРµ СЃРѕР·РґР°СЃС‚ РїСѓСЃС‚СѓСЋ РїР°РїРєСѓ Р±РµР· С„Р°Р№Р»РѕРІ.

Р›РѕРєР°Р»СЊРЅРѕ:

```bash
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Seb0g1/seb0g1.dev___loov.git
git push -u origin main
```

Р•СЃР»Рё СЂРµРїРѕР·РёС‚РѕСЂРёР№ СѓР¶Рµ РµСЃС‚СЊ РЅР° СЃРµСЂРІРµСЂРµ Рё РІ РЅС‘Рј РµСЃС‚СЊ `.git`:

```bash
git pull origin main
```

Р•СЃР»Рё СЃС‚Р°РІРёС‚Рµ СЃ РЅСѓР»СЏ РЅР° СЃРµСЂРІРµСЂРµ:

```bash
git clone https://github.com/Seb0g1/seb0g1.dev___loov.git
cd seb0g1.dev___loov
cp .env.example .env
nano .env
```

Р—Р°РїСѓСЃРє Docker:

```bash
COMPOSE_PROJECT_NAME=seb0g1loov docker compose up -d --build
```

Р•СЃР»Рё Сѓ РІР°СЃ СЃС‚Р°СЂР°СЏ РІРµСЂСЃРёСЏ Docker Рё `docker compose` РЅРµ СЂР°Р±РѕС‚Р°РµС‚, РёСЃРїРѕР»СЊР·СѓР№С‚Рµ:

```bash
COMPOSE_PROJECT_NAME=seb0g1loov docker-compose up -d --build
```

РџСЂРѕРІРµСЂСЊС‚Рµ, С‡С‚Рѕ РїРѕСЂС‚ `7070` РѕС‚РєСЂС‹С‚ РІ firewall.

## GitHub push

Р•СЃР»Рё СЂРµРїРѕР·РёС‚РѕСЂРёР№ РµС‰С‘ РЅРµ РёРЅРёС†РёР°Р»РёР·РёСЂРѕРІР°РЅ Р»РѕРєР°Р»СЊРЅРѕ:

```bash
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Seb0g1/seb0g1.dev___loov.git
git push -u origin main
```

## РЎС‚СЂСѓРєС‚СѓСЂР°

- `backend/` вЂ” FastAPI, Telegram, Р‘Р”, РіРµРЅРµСЂР°С†РёСЏ, РїСѓР±Р»РёРєР°С†РёРё
- `frontend/` вЂ” Vue 3 + Vite + TypeScript
- `storage/` вЂ” С„Р°Р№Р»С‹, РёР·РѕР±СЂР°Р¶РµРЅРёСЏ, Р°СЂС‚РµС„Р°РєС‚С‹
## Р”РµРїР»РѕР№ СЃ РЅСѓР»СЏ РІ `/home`

1. РќР° СЃРІРѕРµР№ РјР°С€РёРЅРµ Р·Р°РїСѓС€СЊС‚Рµ РєРѕРґ РІ GitHub.
2. Р—Р°Р№РґРёС‚Рµ РЅР° СЃРµСЂРІРµСЂ РїРѕ SSH.
3. Р’С‹РїРѕР»РЅРёС‚Рµ:

```bash
cd /home
git clone https://github.com/Seb0g1/seb0g1.dev___loov.git
cd /home/seb0g1.dev___loov
cp .env.example .env
nano .env
```

4. Р’ `.env` Р·Р°РјРµРЅРёС‚Рµ РІСЃРµ `replace_with_your_...` РЅР° СЃРІРѕРё Р·РЅР°С‡РµРЅРёСЏ.
5. Р—Р°РїСѓСЃС‚РёС‚Рµ:

```bash
COMPOSE_PROJECT_NAME=seb0g1loov docker compose up -d --build
```

Р•СЃР»Рё СЃРµСЂРІРµСЂ СЃС‚Р°СЂС‹Р№ Рё РєРѕРјР°РЅРґР° РЅРµ СЂР°Р±РѕС‚Р°РµС‚:

```bash
COMPOSE_PROJECT_NAME=seb0g1loov docker-compose up -d --build
```

6. РћС‚РєСЂРѕР№С‚Рµ РїР°РЅРµР»СЊ:

- `http://IP_РЎР•Р Р’Р•Р Рђ:7070`

7. Р§С‚РѕР±С‹ РѕР±РЅРѕРІРёС‚СЊ РїРѕР·Р¶Рµ:

```bash
cd /home/seb0g1.dev___loov
git pull origin main
COMPOSE_PROJECT_NAME=seb0g1loov docker compose up -d --build
```




## Панель настроек

Рабочие параметры AI, ботов, фидов, автопостинга и интервалов теперь можно менять прямо в админке. В .env остаются только стартовые вещи: база, CORS и базовые параметры запуска.


## Быстрый старт на сервере

Если репозиторий уже лежит в /home, можно просто запустить:

```bash
bash scripts/deploy_home.sh
```

Скрипт сам проверит Docker, поднимет базу, дождётся готовности PostgreSQL и запустит backend + frontend.


## Категории проектов

У каждого проекта теперь есть свой список категорий. Его можно задать в админке в карточке проекта, а импорт будет брать только подходящие товары из Ozon, Wildberries и Yandex Market.

